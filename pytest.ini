[pytest]
asyncio_mode = auto
markers =
    unit: Unit tests (fast, no external dependencies)
    integration: Integration tests (require external services)
    e2e: End-to-end tests (require system dependencies like compilers)
testpaths = tests
pythonpath = src
